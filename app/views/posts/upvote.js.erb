$('.upvote').find('a img').attr({
    src: '/assets/unvote.png',
    alt: 'Unvote'
});
$('.upvote').find('a').attr('data-method', 'delete');
$('.upvote').find('a').attr('href', 'posts/<%= @post.id %>/unvote');
$('.net_val').text('<%= @post.votes.length %>');