$('.upvote').find('a img').attr({
    src: '/assets/vote.png',
    alt: 'Vote'
});
$('.upvote').find('a').attr('data-method', 'put');
$('.upvote').find('a').attr('href', 'posts/<%= @post.id %>/upvote');
$('.net_val').text('<%= @post.votes.length %>');