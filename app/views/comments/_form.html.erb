<%= simple_form_for @comment do |f| %>

    <%= f.input :content %>
    <%= f.check_box :name, {}, "true", "false" %>
    <%= f.hidden_field :post_id, value: @post.id %>
    <%= f.submit 'Comment' %>

<% end %>
