alert("lol")
// $('.upvote').find('a').text('v')
// $('.upvote').find('a').attr("href", "http://localhost:3000/votes?vote%5Bpost_id%5D=#{post.id} %>")
// $('.net_val').text(<%= @post.net_val.to_s %>)